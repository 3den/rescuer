{
  "name": "try-er",
  "version": "0.0.2",
  "description": "High Order Function that wraps any function and returns a default value if it fails.",
  "main": "lib/tryer.js",
  "keywords": [
    "errors",
    "try",
    "catch"
  ],
  "repository": "https://github.com/3den/tryer",
  "bugs": "https://github.com/3den/tryer/issues",
  "author": "Marcelo Eden <eden@3den.org>",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jasmine",
    "prepublish": "yarn run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "babel-register": "^6.26.0",
    "jasmine": "^2.8.0"
  }
}
