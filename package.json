{
  "name": "rescuer",
  "version": "0.0.3",
  "description": "Higher-order function that wraps any function and saves it from unwanted errors.",
  "main": "lib/rescuer.js",
  "keywords": [
    "errors",
    "try",
    "catch",
    "rescue"
  ],
  "repository": "https://github.com/3den/rescuer",
  "bugs": "https://github.com/3den/rescuer/issues",
  "author": "Marcelo Eden <spam@3den.me>",
  "license": "MIT",
  "scripts": {
    "build": "babel src -d lib",
    "test": "jasmine",
    "prepublish": "yarn run build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "babel-register": "^6.26.0",
    "jasmine": "^2.8.0"
  }
}
